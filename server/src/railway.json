{
  "builds": [
    {
      "src": "package.json",
      "use": "@railway/node@latest",
      "config": {
        "buildCommand": "npm install && npm run build",
        "startCommand": "npm start"
      }
    }
  ]
}
